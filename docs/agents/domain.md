# Domain Docs

This repository uses a single-context domain documentation layout.

Before exploring an area, read `CONTEXT.md` at the repository root when it exists and any relevant ADRs under `docs/adr/`. Missing domain files are expected and should not block work.

Use vocabulary defined in `CONTEXT.md`. If a proposed change conflicts with an ADR, surface the conflict explicitly rather than silently overriding it.
